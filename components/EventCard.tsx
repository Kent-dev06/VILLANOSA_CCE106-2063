import { Pressable, StyleSheet, Text, View } from 'react-native';
import { Event } from '@/data/events';
export function EventCard({ event, onPress }: { event: Event; onPress: () => void }) { return <Pressable onPress={onPress} style={({ pressed }) => [styles.card, pressed && styles.pressed]}><View style={styles.row}><Text style={styles.category}>{event.category.toUpperCase()}</Text>{event.joined && <Text style={styles.joined}>JOINED</Text>}</View><Text style={styles.title}>{event.title}</Text><Text style={styles.details}>{event.date} • {event.time}</Text><Text style={styles.venue}>{event.venue}</Text></Pressable>; }
const styles = StyleSheet.create({ card: { backgroundColor: '#FFF', borderColor: '#E2EBE5', borderRadius: 16, borderWidth: 1, marginBottom: 12, padding: 16 }, pressed: { opacity: .72 }, row: { flexDirection: 'row', justifyContent: 'space-between' }, category: { color: '#2D6A4F', fontSize: 11, fontWeight: '800', letterSpacing: .7 }, joined: { color: '#B45309', fontSize: 11, fontWeight: '800' }, title: { color: '#173A2B', fontSize: 18, fontWeight: '800', marginTop: 7 }, details: { color: '#42584B', fontSize: 14, fontWeight: '600', marginTop: 8 }, venue: { color: '#748178', fontSize: 13, marginTop: 3 } });
