import { Link } from 'expo-router';
import { ScrollView, StyleSheet, Text, useWindowDimensions, View } from 'react-native';
import { StatCard } from '@/components/StatCard'; import { events } from '@/data/events';
export default function HomeScreen() { const { width } = useWindowDimensions(); const joined = events.filter((event) => event.joined).length; const upcoming = events.filter((event) => event.date.includes('September')).length; return <ScrollView style={styles.screen} contentContainerStyle={styles.content}><Text style={styles.kicker}>EVENTMATE</Text><Text style={styles.title}>Hi, Kent! 👋</Text><Text style={styles.sub}>Your campus life, all in one place.</Text><View style={[styles.stats, width < 420 && styles.statsNarrow]}><StatCard label="Total events" value={events.length} tint="#2D6A4F" /><StatCard label="Joined events" value={joined} tint="#D97706" /><StatCard label="Upcoming" value={upcoming} tint="#2563EB" /></View><View style={styles.feature}><Text style={styles.featureLabel}>NEXT UP</Text><Text style={styles.featureTitle}>Campus Career Fair</Text><Text style={styles.featureText}>September 22 · Student Center Hall</Text><Link href={'/events' as any} style={styles.link}>Browse all events →</Link></View></ScrollView>; }
const styles = StyleSheet.create({ screen: { flex: 1, backgroundColor: '#F7FAF8' }, content: { padding: 22, paddingBottom: 40 }, kicker: { color: '#2D6A4F', fontSize: 12, fontWeight: '800', letterSpacing: 1.5 }, title: { color: '#173A2B', fontSize: 31, fontWeight: '800', marginTop: 5 }, sub: { color: '#64746A', fontSize: 16, marginTop: 6 }, stats: { flexDirection: 'row', gap: 10, marginTop: 28 }, statsNarrow: { flexWrap: 'wrap' }, feature: { backgroundColor: '#DCEFE4', borderRadius: 20, marginTop: 28, padding: 22 }, featureLabel: { color: '#2D6A4F', fontSize: 11, fontWeight: '800', letterSpacing: 1 }, featureTitle: { color: '#173A2B', fontSize: 22, fontWeight: '800', marginTop: 8 }, featureText: { color: '#426251', marginTop: 6 }, link: { color: '#1F5A40', fontSize: 15, fontWeight: '800', marginTop: 20 } });
